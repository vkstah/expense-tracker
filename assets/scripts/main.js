const balance = document.getElementById('balance');
const form = document.getElementById('form');

// Function to add a new transaction to the transaction list. This function also takes care of adding to either the total income or expense counter.
function addTransaction(e) {
    e.preventDefault();
}